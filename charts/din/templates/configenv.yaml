apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "din.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "din.labels" . | indent 4 }}
data:
{{- range $key, $val := .Values.env }}
  {{ $key }} : {{ $val | quote}}
{{- end}}
